# 카펫

#완전탐색 #알고리즘 #코딩테스트

# 문제 난이도 중

## 1. 📊 객관적 분석 및 근거

### 1.1 제약 조건 파악

| 항목 | 값 | 의미 |
|------|-----|------|
| brown (갈색 격자 수) | 8 ~ 5,000 | 테두리 격자 수 |
| yellow (노란색 격자 수) | 1 ~ 2,000,000 | 내부 격자 수 |
| 시간 제한 | - | 통상 1초 가정 |
| 메모리 제한 | - | 특별한 제약 없음 |

### 1.2 시간 복잡도 역산

**기준**: 1초 ≈ 10^8 연산

| 복잡도 | N=2,005,000 | 가능 여부 |
|--------|-------------|----------|
| O(√N) | ~1,416 | ✅ 매우 여유로움 |
| O(N) | 2,005,000 | ✅ 충분 |
| O(N log N) | ~42,000,000 | ✅ 가능 |
| O(N²) | 4×10^12 | ❌ 불가능 |

**이 문제의 경우**:
- 전체 격자 수 = brown + yellow ≤ 5,000 + 2,000,000 = 2,005,000
- 약수 쌍을 찾는 것이므로 √N까지만 탐색하면 됨 → O(√N) ≈ 1,416회
- **결론**: O(√N) 알고리즘으로 충분

### 1.3 문제 패턴 인식

**핵심 질문**: 이 문제가 요구하는 것은 무엇인가?

| 관찰 포인트 | 이 문제에서 | 시사점 |
|-------------|-------------|--------|
| 최적해 vs 모든 경우 | 조건을 만족하는 경우 찾기 | 완전탐색으로 모든 약수 쌍 검사 |
| 순서가 중요한가 | 아니오 | 가로 ≥ 세로 조건만 만족하면 됨 |
| 수학적 관계가 존재하는가 | 예 (넓이 = 가로 × 세로) | 약수 분해 활용 |
| 제약 조건이 좁은가 | 예 (테두리 조건) | 후보를 빠르게 검증 가능 |

### 1.4 알고리즘 선택 및 논증

#### 🎯 최종 선택: 완전탐색 (약수 탐색)

**선택 근거 (Why this algorithm?)**

1. **문제 구조와의 적합성**
   - 카펫의 가로 × 세로 = brown + yellow (전체 넓이)
   - 전체 넓이의 약수 쌍만 후보가 될 수 있음
   - 약수 개수는 많아야 수백 개 수준이므로 전부 확인해도 빠름

2. **제약 조건 충족**
   - 시간 복잡도: O(√N) → N=2,005,000일 때 약 1,400회 연산 (✅ 통과)
   - 공간 복잡도: O(1) → 추가 메모리 거의 없음 (✅ 통과)

3. **핵심 인사이트**
   - 노란색 영역은 테두리(갈색)를 제외한 내부에 위치
   - 따라서 노란색 넓이 = (가로 - 2) × (세로 - 2)
   - 이 조건을 만족하는 약수 쌍을 찾으면 정답

#### ❌ 기각된 대안들

| 대안 | 복잡도 | 기각 이유 |
|------|--------|-----------|
| 이분탐색 | O(log N) | 조건이 단조 함수가 아니므로 이분탐색 조건 미충족 |
| 수학 공식 풀이 | O(1) | 2차 방정식으로 풀 수 있으나, 약수 탐색이 더 직관적 |

#### 💡 비슷한 문제에서 이 패턴을 인식하는 법

- **키워드**: "직사각형", "넓이", "테두리", "가로 세로"
- **문제 유형**: 넓이/약수 관계를 활용한 완전탐색
- **주의사항**:
    - 가로 ≥ 세로 조건을 놓치지 말 것
    - 세로는 최소 3 이상 (노란색이 1개라도 있으려면)
    - 제곱근까지만 탐색하여 효율성 확보

---

## 2. 🧠 자연어 실행 흐름

1. **전체 넓이 계산**: total = brown + yellow
2. **세로 후보 탐색 시작**: 세로(height)는 3부터 √total까지 순회
   - 세로가 3 이상이어야 내부에 노란색이 들어갈 공간이 생김
3. **약수 검증**: total이 height로 나누어 떨어지는지 확인
4. **가로 계산**: width = total ÷ height
5. **가로 ≥ 세로 조건 확인**: width ≥ height인지 검사
6. **노란색 조건 검증**: (width - 2) × (height - 2) == yellow인지 확인
7. **조건 만족 시 반환**: [width, height] 형태로 반환
8. **모든 후보 실패 시**: 빈 배열 반환 (문제 조건상 항상 답이 존재)

---

## 3. 💻 Code Implementation

(언어: Python)
```python
def solution(brown, yellow):
    # 1. 전체 격자 수 계산
    total = brown + yellow
    
    # 2. 세로 길이 후보 탐색 (3부터 √total까지)
    #    - 세로가 3 이상이어야 노란색이 존재할 수 있음
    #    - 가로 >= 세로 조건이므로, 세로는 √total 이하
    for height in range(3, int(total ** 0.5) + 1):
        # 3. 전체 넓이가 height로 나누어 떨어지는 경우만 유효
        if total % height == 0:
            width = total // height
            
            # 4. 가로 >= 세로 조건 확인
            if width >= height:
                # 5. 노란색 영역 조건 검증
                #    테두리 1줄이 갈색이므로, 내부 = (가로-2) × (세로-2)
                if (width - 2) * (height - 2) == yellow:
                    return [width, height]
    
    # 문제 조건상 항상 답이 존재하므로 여기 도달하지 않음
    return []
```

**복잡도 분석**
- 시간: O(√(brown + yellow)) — 약수 탐색은 제곱근까지만 수행
- 공간: O(1) — 추가 메모리 사용 없음

---

# 평가

## 개선할 점
- **나누기 연산**: `int(total / height)` 대신 `total // height` (정수 나누기)를 사용하면 더 명확하고 타입 변환 오버헤드가 없습니다.
- **조건문 스타일**: `if not width >= height`보다 `if width < height`가 더 직관적이고 읽기 쉽습니다.
- **continue 패턴**: 현재 continue를 사용하여 early return 스타일로 작성했는데, 이는 좋은 습관입니다. 다만 중첩 if문으로 작성하는 것과 비교해서 상황에 맞게 선택하면 됩니다.

## 잘한 점
- **자연어 → 코드 변환**: README의 자연어 실행 흐름을 7단계 주석으로 1:1 대응시켜 구현한 점이 매우 훌륭합니다. 이런 습관은 복잡한 문제에서도 논리적 오류를 줄여줍니다.
- **명확한 로직 순서**: 약수 검증 → 가로 계산 → 크기 조건 → 노란색 조건 순으로 자연스럽게 필터링합니다.
- **간결한 코드**: 불필요한 변수나 로직 없이 핵심만 담아 23줄로 완성했습니다.
- **예외 케이스 인지**: 주석에 "예외 케이스는 없음"을 명시하여 문제 조건을 정확히 이해했음을 보여줍니다.

## 다른 응용 방안
- **약수 쌍 탐색**: 두 수의 곱이 특정 값이 되는 쌍 찾기
- **직사각형 관련 문제**: 둘레/넓이로 가로세로 역산
- **타일 채우기**: 주어진 넓이를 특정 조건으로 분할

## 추천 문제
- **프로그래머스: 최소직사각형** — 유사한 가로/세로 조건 문제
- **백준 1051: 숫자 정사각형** — 정사각형 조건 탐색
- **백준 2477: 참외밭** — 도형 넓이 계산
- **프로그래머스: 멀쩡한 사각형** — 넓이와 수학적 관계

## 종합 평가
README의 "자연어 실행 흐름"을 그대로 코드의 주석으로 옮긴 뒤 구현하는 방식은 **메타인지적 문제 해결**의 좋은 예시입니다.
문제를 이해하고 → 단계별로 분해하고 → 코드로 변환하는 과정이 명확하게 드러납니다.
다만 `int(total / height)`를 `//` 연산자로, `not width >= height`를 `width < height`로 바꾸면 Python 관용 표현에 더 가까워집니다.
전체적으로 군더더기 없는 깔끔한 풀이입니다.
