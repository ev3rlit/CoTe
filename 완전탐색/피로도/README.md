# 피로도

#완전탐색 #알고리즘 #코딩테스트

# 문제 난이도 중

## 1. 📊 객관적 분석 및 근거

### 1.1 제약 조건 파악

| 항목 | 값 | 의미 |
|------|-----|------|
| k (현재 피로도) | 1 ~ 5,000 | 초기 피로도 |
| dungeons 개수 | 1 ~ 8 | 던전의 최대 개수가 매우 작음 → 완전탐색 가능 |
| 최소 필요 피로도 | 1 ~ 1,000 | 입장 조건 |
| 소모 피로도 | 1 ~ 1,000 | 항상 최소 필요 피로도 이하 |
| 시간 제한 | - | 통상 1초 가정 |

### 1.2 시간 복잡도 역산

**기준**: 1초 ≈ 10^8 연산

**순열 개수 분석**:
- 던전 방문 순서가 결과에 영향을 미침
- N개 던전의 모든 방문 순서 = N!

| N (던전 수) | N! (순열 수) | 가능 여부 |
|-------------|-------------|----------|
| 8 | 40,320 | ✅ 매우 여유로움 |

**전체 복잡도**: O(N! × N) = 40,320 × 8 ≈ 32만 연산 → ✅ 통과

### 1.3 문제 패턴 인식

**핵심 질문**: 이 문제가 요구하는 것은 무엇인가?

| 관찰 포인트 | 이 문제에서 | 시사점 |
|-------------|-------------|--------|
| 최적해 vs 모든 경우 | 최대 던전 수 (최적해) | 완전탐색 or 그리디 |
| 순서가 중요한가 | 예 (던전 순서에 따라 결과 달라짐) | 순열(Permutation) 필요 |
| 그리디 가능한가 | 아니오 | 반례 존재 (문제 예시 참고) |
| 입력 크기 | N ≤ 8 (매우 작음) | 완전탐색 확정 |

### 1.4 알고리즘 선택 및 논증

#### 🎯 최종 선택: 완전탐색 (순열 생성)

**선택 근거 (Why this algorithm?)**

1. **문제 구조와의 적합성**
   - 던전 방문 순서에 따라 탐험 가능한 던전 수가 달라짐
   - 첫 번째 예시에서 1→2→3 순서는 2개, 1→3→2 순서는 3개 탐험 가능
   - 그리디로는 최적해를 보장할 수 없음 → 모든 순서 확인 필요

2. **제약 조건 충족**
   - 시간 복잡도: O(N! × N) → N=8일 때 약 32만 연산 (✅ 통과)
   - 공간 복잡도: O(N) → 순열 생성 시 현재 순열만 저장 (✅ 통과)

3. **핵심 인사이트**
   - N ≤ 8이라는 제약이 완전탐색을 암시
   - "최대 던전 수"를 구해야 하므로 모든 경우를 확인해야 함

#### ❌ 기각된 대안들

| 대안 | 복잡도 | 기각 이유 |
|------|--------|-----------|
| 그리디 (최소 필요 피로도 높은 순) | O(N log N) | 반례 존재. 문제 예시에서 순서에 따라 결과가 다름 |
| 그리디 (소모 피로도 낮은 순) | O(N log N) | 마찬가지로 반례 존재 |
| DP (비트마스킹) | O(2^N × N) | 가능하지만 N=8에서 순열이 더 직관적 |

#### 💡 비슷한 문제에서 이 패턴을 인식하는 법

- **키워드**: "최대 개수", "방문 순서", "던전 N개 (N ≤ 10)"
- **문제 유형**: 순열 기반 완전탐색
- **주의사항**:
    - N ≤ 10~12 정도면 순열 완전탐색 고려
    - 순서가 결과에 영향을 미치는지 확인
    - 그리디로 풀리는지 반례 검증 필수

---

## 2. 🧠 자연어 실행 흐름

1. **모든 던전 방문 순서 생성**: itertools.permutations로 N!개의 순열 생성
2. **각 순열에 대해 시뮬레이션**:
   - 현재 피로도를 k로 초기화
   - 순열 순서대로 던전을 순회
3. **던전 입장 가능 여부 판단**:
   - 현재 피로도 ≥ 최소 필요 피로도면 입장
   - 입장 후 소모 피로도만큼 차감
   - 탐험 횟수 증가
4. **입장 불가 시 중단**: 해당 순열에서의 탐험은 종료
5. **최대값 갱신**: 현재 순열에서의 탐험 횟수와 기존 최대값 비교
6. **결과 반환**: 모든 순열 탐색 후 최대 던전 수 반환

---

## 3. 💻 Code Implementation

(언어: Python)
```python
from itertools import permutations

def solution(k, dungeons):
    max_count = 0
    
    # 1. 모든 던전 방문 순서 생성
    for order in permutations(dungeons, len(dungeons)):
        current = k      # 현재 피로도
        count = 0        # 탐험한 던전 수
        
        # 2. 각 순서대로 던전 탐험 시도
        for threshold, cost in order:
            # 3. 입장 가능 여부 판단
            if current >= threshold:
                current -= cost
                count += 1
            else:
                # 4. 입장 불가시 중단
                break
        
        # 5. 최대값 갱신
        max_count = max(max_count, count)
    
    return max_count
```

**복잡도 분석**
- 시간: O(N! × N) — N=8일 때 약 32만 연산
- 공간: O(N) — 순열 한 개씩만 메모리에 유지

---

# 평가

## 개선할 점
- **조건문 스타일**: `if not current >= threshold`보다 `if current < threshold`가 더 직관적입니다. 이중 부정은 가독성을 해칩니다.
- **변수명 선택**: `max_explorer`보다 `max_count` 또는 `max_dungeons`가 의미를 더 명확하게 전달합니다. "explorer"는 탐험가를 의미하므로 던전 개수와 혼동될 수 있습니다.
- **조기 종료 최적화**: 만약 `count == len(dungeons)`면 모든 던전을 탐험한 것이므로 더 이상 탐색할 필요가 없습니다. 이 경우 즉시 반환하면 불필요한 순열 탐색을 줄일 수 있습니다.

## 잘한 점
- **문제 분석 주석**: 코드 상단에 입력 조건, 알고리즘 선택 이유, 시간 복잡도(8! = 40,320)를 명확히 기록했습니다. 실제 코테에서 사고 과정을 정리하는 좋은 습관입니다.
- **단계별 흐름 주석**: 1~4단계로 로직을 분리한 주석이 코드 이해를 돕습니다.
- **튜플 언패킹**: `for (threshold, cost) in case` 패턴으로 가독성 있는 순회를 했습니다.
- **정확한 알고리즘 선택**: N ≤ 8이라는 제약을 보고 순열 완전탐색을 바로 떠올린 점이 좋습니다.

## 다른 응용 방안
- **일정 스케줄링**: 작업 순서에 따라 완료 가능한 작업 수가 달라지는 문제
- **배터리 소모 게임**: 배터리로 여러 미션을 수행할 때 최대 미션 수
- **자원 관리 시뮬레이션**: 제한된 자원으로 최대 행동 수행

## 추천 문제
- **프로그래머스: 모음사전** — 순열 기반 완전탐색
- **백준 10819: 차이를 최대로** — 순열로 최적해 탐색
- **백준 10971: 외판원 순회 2** — 순열 + 최적화
- **프로그래머스: 불량 사용자** — 순열 + 집합 조합

## 종합 평가
문제의 핵심인 "순서가 중요하다"와 "N이 작다"를 정확히 파악하여 순열 완전탐색을 선택했습니다.
주석에서 "그리디가 안 되는 이유"를 명시하지는 않았지만, 코드 상단의 분석 흔적에서 충분한 고민이 보입니다.
다만 `if not current >= threshold`와 같은 이중 부정 표현은 실수를 유발할 수 있으므로, 
`if current < threshold: break` 형태로 바꾸는 것을 권장합니다.
