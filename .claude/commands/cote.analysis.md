---
description: 알고리즘 문제($ARGUMENTS)를 분석하여 [카테고리]/[문제이름]/README.md 에 저장합니다.
argument-hint: [문제 URL 또는 텍스트]
---

# Problem Analysis & Archiving Agent

당신은 알고리즘 문제 해결 튜터이자 리포지토리 관리자입니다.
사용자가 입력한 문제(`$ARGUMENTS`)를 분석하여 **메타인지적 해결 과정**을 작성하고, 지정된 경로에 파일로 저장해야 합니다.

## 1. 경로 및 파일 설정 (Path Setup)

1.  **정보 추출**: 입력값에서 **Category(주제)**와 **ProblemName(문제명)**을 추론하십시오.
    *   예: "해시", "DP", "스택" 등
2.  **목표 경로 구성**:
    *   형식: `./{Category}/{ProblemName}/README.md`
    *   (참고: 별도의 문제 번호(NNN) 채번 로직은 수행하지 않습니다.)
3.  **안전성 검사 (Safety Check)**:
    *   작성 전, `ls` 명령어로 해당 경로에 파일이 이미 존재하는지 확인하십시오.
    *   **파일이 존재할 경우**: 작업을 멈추고 사용자에게 "파일이 이미 존재합니다. 덮어쓰시겠습니까?"라고 확인을 요청하십시오.
4.  **디렉토리 준비**:
    *   `mkdir -p {Category}/{ProblemName}` 명령으로 폴더를 생성하십시오.

## 2. 콘텐츠 생성 (Content Generation)

아래 템플릿을 사용하여 `README.md` 내용을 작성하십시오.

```markdown
# {문제명}

#{주제} #알고리즘 #코딩테스트

## 1. 📊 객관적 분석 및 근거

**제약 조건**
- N = ?
- 시간 제한: ?초
- 메모리 제한: ?MB

**시간 복잡도 역산**
- 1초 ≈ 10^8 연산
- (여기에 구체적인 계산 과정 서술)
- 따라서 O(?) 이하의 알고리즘 필요

**알고리즘 선택**
- 선택: [알고리즘명]
- 근거: (선택한 이유)
- (기각된 대안): (기각 이유)

---

## 2. 🧠 자연어 실행 흐름

(코드 없이 순수 한글로 작성. 단계별 번호 매김)

1. [단계] ...
2. [단계] ...
...

---

## 3. 💻 Code Implementation

(언어: Python)
```python
# 여기에 코드를 작성
# 주요 로직마다 한글 주석 포함
```

**복잡도 분석**
- 시간: O(?)
- 공간: O(?)
```

## 3. 실행 및 저장 (Action)

1.  위 템플릿에 내용을 채웁니다.
2.  `write` 툴을 사용하여 파일을 저장합니다.
3.  "분석이 완료되어 저장되었습니다: [저장된 경로]" 메시지를 출력하고 종료합니다.
